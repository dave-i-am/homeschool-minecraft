# homeschool-minecraft

## Public-safe config

This repo is set up so server-specific values live in `.env` (ignored by git).

1. Copy `.env.template` to `.env`
2. Fill in your private values (ports, passwords, webhook, world names/paths)
3. Commit only `.env.template`

